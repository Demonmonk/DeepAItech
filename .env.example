# Deep AI Tech — environment variables
# Copy to `.env.local` for local dev, or set these in your host (e.g. Vercel
# → Project Settings → Environment Variables). Never commit real secrets.

# --- AI chat assistant (app/api/chat) ---------------------------------------
# Anthropic API key. Without it, the chat widget stays up but politely says
# it isn't connected yet. Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional. Model used by the assistant. Defaults to the cheapest current-gen
# model (Claude Haiku). For absolute lowest cost you can use an older Haiku,
# e.g. claude-3-haiku-20240307
# CHAT_MODEL=claude-haiku-4-5

# --- Contact form delivery (app/api/contact) --------------------------------
# Configure ONE (or both). If neither is set, submissions are still logged
# server-side, and the form still returns success.

# Option 1 — Email via Resend (https://resend.com). Create an API key, and
# (recommended) verify your sending domain so you can use a real "from".
# RESEND_API_KEY=
# CONTACT_TO=hello@deepaitech.ai            # where enquiries are sent
# CONTACT_FROM=Deep AI Tech <hello@deepaitech.ai>   # must be on a verified domain

# Option 2 — Incoming webhook (Slack or Discord). Paste the webhook URL.
# CONTACT_WEBHOOK_URL=
